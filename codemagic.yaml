workflows:
  boardly-workflow:
    name: Boardly Android Build

    environment:
      java: 17

    scripts:
      - chmod +x gradlew
      - ./gradlew clean
      - ./gradlew assembleDebug

    artifacts:
      - app/build/outputs/**/*.apk
